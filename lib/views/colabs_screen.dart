import 'package:flutter/material.dart';

class ColabsScreen extends StatelessWidget {
  ColabsScreen();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('COLABORADORES'),
    );
  }
}
